defaults:
  - model: ContrastiveModel
  - datamodule: DataModuleBasic
  - split: split_mouse
  - _self_

wandb:
  enabled: true
  run_name: weighted_run
  entity: iacopodipippo-technical-university-of-munich
  project: scConcept

initialize:  
  resume: False
  run_id: null
  checkpoint: null

profiler:
  enabled: False # Use profiler only for debugging
  
PATH:
  PROJECT_PATH: /p/scratch/cjinm16/dipippo1/scConcept
  PROJECT_DATA_PATH: /p/project1/hai_fzj_bda/spitzer2/point_transformer/data/raw
  CHECKPOINT_ROOT: ${PATH.PROJECT_PATH}/model_checkpoints
  DATASET_PATH: ${PATH.PROJECT_DATA_PATH}
  ADATA_PATH: ${PATH.DATASET_PATH}
  PANELS_PATH: ${PATH.PROJECT_PATH}/panels/
  SPLIT: ${split}
  gene_mapping_path: ${PATH.PROJECT_PATH}/token_mapping/pc_gene_token_mapping.pkl



# Project directory structure:
# PROJECT_PATH/
# ├── data/
# │   └── <dataset_name>/
# │       ├── pc_gene_token_mapping.pkl
# │       └── h5ads/
# │           ├── <adata_1>.h5ad
# │           ├── <adata_2>.h5ad
# │           └── ...
# ├── model_checkpoints/
# └── panels/
#     └── <panel_name>/
#         ├── panel_1.csv
#         ├── panel_2.csv
#         └── ...
